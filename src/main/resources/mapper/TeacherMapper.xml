<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuegongbu.mapper.TeacherMapper">
    
    <resultMap id="BaseResultMap" type="com.xuegongbu.entity.Teacher">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="teacher_no" property="teacherNo"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="department" property="department"/>
        <result column="identity" property="identity"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM tb_teacher WHERE username = #{username}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM tb_teacher WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.xuegongbu.entity.Teacher" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_teacher (username, password, real_name, teacher_no, phone, email, department, identity, status)
        VALUES (#{username}, #{password}, #{realName}, #{teacherNo}, #{phone}, #{email}, #{department}, #{identity}, #{status})
    </insert>

    <update id="update" parameterType="com.xuegongbu.entity.Teacher">
        UPDATE tb_teacher
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="teacherNo != null">teacher_no = #{teacherNo},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="department != null">department = #{department},</if>
            <if test="identity != null">identity = #{identity},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>
